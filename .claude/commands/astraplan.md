---
description: Plan with GPT-6 Astra Ultra through the shared astraplan skill; Claude executes and verifies.
argument-hint: <task to plan>
---

Read `~/.claude/skills/astraplan/SKILL.md` and follow it for **$ARGUMENTS**. Resolve its bundled runner from that skill directory. Claude remains the calling host, executor, and final decision-maker; Astra coordinates read-only planning through the existing Codex CLI. Persist the brief and contract before dispatch. Do not substitute a Claude model for Astra.
