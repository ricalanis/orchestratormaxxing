---
description: Plan with GPT-6 Astra Ultra through the shared astraplan skill.
---

Load the installed `astraplan` skill and follow it for $ARGUMENTS. OpenCode remains the calling host and executor; the skill's paired runner delegates read-only planning to the existing Codex CLI. Do not point an OpenCode provider at a Claude subscription or silently substitute another model.
