Write the acceptance contract and context brief before choosing a worker.
Dispatch both together with `oll "<brief + contract>" --model glm-5.2`.
Root Codex verifies the worker response and performs final sign-off.
