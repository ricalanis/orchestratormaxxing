{
  "name": "hermes-orchestrator-dashboard",
  "version": "0.0.0",
  "private": true,
  "description": "Dashboard JS unit tests (jsdom). The app itself is Python/FastAPI; this is only for the browser-side logic in dashboard/static/.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "jsdom": "^26.1.0"
  }
}
